<!DOCTYPE html>
<html lang="<%= espanol ? "es":"en" %>" xmlns="http://www.w3.org/1999/xhtml"
      xmlns:fb="http://ogp.me/ns/fb#">
<head>
  <% include ../partials/header.ejs %>

  <script>
  var currentPage = "<%= page %>";
  var currentNav = '.' + currentPage.substring(7) + '.nav-indiv';
  var mobileNav = ".overlay-content ." + currentPage.substring(7);

  //get just page name
  // var where = currentPage.lastIndexOf('/');
  // var cleanPage = currentPage.substring(where+1);
  </script>
  <meta property="og:image" content="http://cubayouth.herokuapp.com/assets/images/feature/<%= pageName %>.jpg" />
  <% if (page.includes('harbor')){ %>
    <title>Across the Harbor: A young Cuban who works in tourism commutes daily from Regla - Cuba's New Wave</title>
  <% } else if (page.includes('dorian')){ %>
    <title>Cutting Edge: A trendsetting entrepreneur opens a hair salon in Havana, Cuba - Cuba's New Wave</title>
  <% } else if (page.includes('family')){ %>
    <title>Viva La Familia: Diego and his grandmother find common ground in a changing Cuba - Cuba's New Wave</title>
  <%   } else if (page.includes('hiphop')){ %>
    <title>The Real Queens: A multimedia feature about two female hip-hop artists in Havana, Cuba - Cuba's New Wave</title>
  <%   } else if (page.includes('skate')){ %>
    <title>Revolution on Wheels: A brother and sister in Havana, Cuba pursue their dreams as skateboarders - Cuba's New Wave</title>
  <%   } else { %>
    <title>Cuba’s New Wave: Multimedia Feature from UNC School of Media and Journalism - Cuba's New Wave</title>
  <% } %>



</head>

<body>

	<%  include ../partials/nav.ejs %>
	<% include ../partials/article.ejs %>
	<%  include ../partials/footer.ejs %>
  <script type="text/javascript" src="/js/inner.js"></script>
</body>
</html>
